<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/7.0.1/css/all.min.css">
    <style>
        body {
            background-image: linear-gradient(rgba(0, 0, 0, .5), rgba(0, 0, 0, .5)), url('/lain_wall.jpg');
            /* font-family: Windows Regular, sans-serif; */
            background-size: cover;
            font-family: "Segoe UI Semilight", Tahoma, Geneva, Verdana, sans-serif, Arial, Helvetica, sans-serif;
        }

        .gray {
            color: #928374
        }

        .box {
            /* background: linear-gradient(90deg, silver, #d2d2d2, silver); */
            /* border-top: 2px solid #D9D9D9; */
            /* box-shadow: inset 0 2px #fff; */
            /* height: fit-content; */
        }

        .styled-container {
            background: rgba(27, 27, 27, 0.8);
            box-shadow: rgba(0, 0, 0, 0.3) 0px 3px 8px;
            border-radius: 5px;
            padding: 10px 10px 10px 10px;
            max-width: 935px;
        }

        .glass-borders {
            background-image: url("/aero_glass.png"), linear-gradient(rgba(48, 48, 48, 0.4), rgba(26, 26, 26, 0.4), rgba(26, 26, 26, 0.4));
            box-shadow: rgba(0, 0, 0, 0.3) 0px 3px 8px;
            border: 1px solid #757575;
            border-radius: 5px;
            padding: 5px;
            margin: 8px auto 8px auto;
        }

        h1 {
            background: linear-gradient(to bottom, rgba(119, 119, 119, 0.925) 0%, rgba(35, 35, 35, 0.925) 55%, rgba(0, 0, 0, 0.925) 55%, rgba(22, 22, 22, 0.925) 100%);
            border: 1px solid black;
            border-radius: 5px;
            padding: 2px 5px 4px 5px;
            margin: 0px auto 0px auto;
            display: flex;
            align-items: center;
            justify-content: center;
            max-width: 970px;
            font-weight: normal !important;
            margin: 0px;
            color: white;
            /* position: relative; */

        }

        .styled-button {
            background: linear-gradient(to bottom, rgba(119, 119, 119, 0.925) 0%, rgba(35, 35, 35, 0.925) 55%, rgba(0, 0, 0, 0.925) 55%, rgba(22, 22, 22, 0.925) 100%);
            border: 1px solid black;
            border-radius: 5px;
            padding: 2px 5px 4px 5px;
            margin: 0px auto 0px auto;
            display: flex;
            align-items: center;
            justify-content: center;
            max-width: 970px;
            font-weight: normal;
            margin: 0px;
            color: whitesmoke
        }

        .aero-borders {
            border: 1px solid #4b4b4b;
        }

        .nav-categories {
            background: rgba(26, 26, 26, 0.486);
            border: 1px solid #424242;
            border-radius: 5px;
            display: flex;
            flex-direction: column;
            padding: 2px 4px;
            margin-bottom: 5px;
        }

        .nav-active,
        .nav-inactive {
            border: 1px solid #0f0f0f;
            border-radius: 5px;
            color: #eeeeee;
            display: flex;
            align-items: center;
            margin: auto auto 2.5px auto;
            width: 93%;
            font-size: 13px;
        }

        .navbar-titles {
            background: linear-gradient(to bottom, rgba(129, 129, 129, 0.8) 0%, rgba(56, 56, 56, 0.8) 50%, rgba(27, 27, 27, 0.8) 50%, rgba(12, 12, 12, 0.8) 100%);
            border: 1px solid #000000;
            border-radius: 4px;
            color: #f7f4f4;
            padding-bottom: 1px;
            margin-bottom: 3px;
            font-size: 12.9px;
            font-weight: normal;
            text-align: center;
        }

        .nav-icons {
            height: 18px;
            margin: 1px 2px 0px 3px;
            z-index: 1;
        }

        .message-box {
            background: linear-gradient(rgba(255, 255, 255, .5));
            border-radius: 5px;
        } 

    </style>
</head>
</head>

<body class="">
    <!-- <header>
        <nav>
            nav here
        </nav>
    </header> -->
    <div class="p-8 grid grid-cols-4 gap-4 justify-evenly">
        <% if (user) { %>

            <aside class="col-span-1">
                <div>
                    <nav class="nav-categories">
                        <span class="navbar-titles">Application Links</span>
                        <div class="nav-inactive">
                            <img src="/chat_room.png" class="nav-icons" />
                            <a href="/users/chat-room" class="pl-2">Chat Room</a>
                        </div>
                        <div class="nav-inactive">
                            <img src="/user.png" class="nav-icons" />
                            <a href="/users/my-profile" class="pl-2">My Profile</a>
                        </div>
                        <div class="nav-inactive">
                            <img src="/dashboard.png" class="nav-icons" />
                            <a href="/users/dashboard" class="pl-2">Dashboard</a>
                        </div>
                        <div class="nav-inactive">
                            <img src="/create_profile.png" class="nav-icons" />
                            <a href="/users/create-profile" class="pl-2">Create Profile</a>
                        </div>
                        <form action="/logout" method="post" style="margin:6px auto; width:93%;">
                            <button type="submit" class="nav-inactive"
                                style="cursor: pointer; width:100%; background:transparent; border:none; text-align:left; padding:6px 8px;">
                                <span class="navbar-titles">Logout</span>
                            </button>
                        </form>
                    </nav>
                </div>
            </aside>
            <% } %>

                <main class="col-span-3">
                    <%- body %>
                </main>
    </div>

</body>

</html>